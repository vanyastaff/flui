## Summary

A rebuild scheduled through the `BuildOwner`'s external inbox **while a build drain is running** — a `RebuildHandle` fired from a `Listenable` notification inside `did_update_view`, a `ChangeNotifier` set by a parent's build hook, an `AnimatedBuilder` subscriber woken by a zero-duration settle (#1181) — used to wait for the next frame and request one: `drain_build_scope` drained the inbox once, before its heap loop, under a comment that said "Flutter defers mid-frame schedules". Flutter defers the *frame request*; the dirty mark joins the same `buildScope` (`BuildScope._scheduleBuildFor` → `_dirtyElements`, re-sorted when `_dirtyElementsNeedsResorting`).

Now the inbox is **absorbed at the top of every heap pop**: an id already in `dirty_reasons` (on the heap or in a deferred scope bucket) merges its reasons; a new id is marked dirty and pushed at its authoritative tree depth, so a shallower element scheduled mid-drain builds next and the redundant next-frame rebuild — and the frame it requested — are gone. The pop-time classification (`nearest_layout_builder_scope` → accept or `defer_dirty_element`) routes absorbed ids exactly as it routes every other entry.

**Bounded.** A *re-entry* — an id that already completed a build in this `build_scope` landing again (a self-rescheduler, a child re-notifying its built parent, an A↔B ping-pong) — is charged against a per-frame budget of 16 (`MAX_MID_DRAIN_ABSORBS`); first-time absorbs are free, so a page whose N independent parents each retarget an implicit animation in one frame costs nothing. When the budget is exhausted the remaining ids stay in the inbox for the next frame (the existing `has_pending_work` gate schedules it), collected in a drain-local accumulator and flushed once, with one `tracing::warn!` per streak (re-armed when a frame ends under the cap). The reset lives in `build_scope`; the lazy-sliver service pass and the layout-builder fixpoint, which re-enter mid-frame, call `build_scope_impl` and share the frame's budget.

No wake suppression: `request_frame` still fires on a newly queued id in any phase — the frame's dirty gate discards the leftover wake, and Flutter's own `onBuildScheduled` latch is recorded as the alternative if a wake-count oracle ever shows a cost. Recorded divergences (`crates/flui-view/ARCHITECTURE.md`, new): Flutter debug-asserts that a mid-build schedule targets a descendant of the building element, FLUI builds any id (the inbox is the only route for listener-driven rebuilds); Flutter drops a `setState` issued during the element's own build, FLUI rebuilds it once more per re-entry up to the budget (a cross-thread schedule cannot tell "during my build" from "after"); Flutter's `_scheduleBuildFor` skips `scheduleRebuild` while `_building`, FLUI requests the frame.

**A masked bug surfaced.** `hero_gesture_tests::a_to_route_that_does_not_maintain_state_falls_back_to_the_deferred_path_without_panicking` had been "corrected" on 2026-08-05 to expect a flight because the covered `maintain_state(false)` destination was found still mounted — that was the inbox's one-frame deferral of the unmount rebuild, not a Navigator gap (`modal_covered_route_without_maintain_state_is_unmounted_and_loses_its_state` already pins the unmount). Probe: `route_subtree(to)` is `Some` on main, `None` under this change. The test is back to its original, Flutter-correct expectation with both episodes recorded; the fixture's false "no controller yet" premise is corrected (the Navigator auto-installs a hero observer whose in-flight flight survives replacement — #1195).

## Tests

flui-view: a descendant scheduled through the inbox from its parent's build builds exactly once in the same scope (a `tree_observer` build counter; two frames → one), a sibling variant, merge into a live entry, shallow-before-deep ordering, the re-entry budget (17 builds then the frame completes and the next scope resumes), first-time absorbs never charged (>16 independent notifications, no warn), the budget shared across a frame's separate drains, streak warn once across two capped frames and once with further pops in the same drain, synchronous `schedule` from a build does not deadlock the inbox lock, a mid-drain schedule still requests a frame (a pin of the recorded non-change), Global/scoped-target routing of absorbed ids, and the generation-carrying `ElementId` making a stale handle an inert miss. Reviewer mutations (absorb only after the heap empties; never charge; charge first-time absorbs; drop the merge branch; reset per drain; warn on every capped absorb) each redden a named test. flui-widgets: the zero-duration implicit retarget leaves `pending_external_builds() == 0` and no dirty element after the retargeting pump.

## Review

Plan attacked twice by harsh-critic (v1's after-heap re-drain kept the redundant build and its `draining` flag was both redundant and wedgeable after a caught build panic; v2 took its ALT-1) and once by concurrency-specialist (a standalone atomic read outside the inbox lock had a real cross-thread lost-wake window — moot once the flag was dropped); the diff by rust-reviewer (mutation probes in a scratch checkout, two rounds) and an outside deepseek-v4.1-flash agentic lens. The reviewer found that the "stale-id hazard" the first cut documented cannot happen (`ElementId` carries a generation) and that the budget reset was per call rather than per frame; the lens found the false "no drain re-enters `build_scope`" claim independently and the duplicated absorb-time routing.

Closes #1180. Refs #1181, #1195.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01G4A6sok5VeWcNfxFTpgXRm
