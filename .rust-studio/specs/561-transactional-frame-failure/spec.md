# Spec: transactional, presentation-local frame failure containment (the remainder of #561)

- **Status:** Draft v1 (approach A′ chosen at the gate on 2026-09-11 with three user answers — see `intent.md` §User amendments)
- **Slug:** `561-transactional-frame-failure`   ·   **Date:** `2026-09-11`   ·   **Owner:** `chief-architect` (design), flui-view / flui-app / flui-rendering leads per PR
- **Governing ADR:** `docs/adr/ADR-0048-frame-transaction-boundary.md` — **amended**, not replaced (the intent says extend); ADR-0027 (presentation-local), ADR-0062 (the paint queue is the cross-pass record), ADR-0054 decision 2 (`GEOMETRY_DEGRADED` sticky stand-in flag)

## Problem

Carried over from `intent.md` ("What's wrong today", after its Corrections): a frame that fails part-way is contained to its presentation and reported, but what it leaves behind is not one version. Concretely, at main `3b8bb79c`:

1. **Build side (flui-view).** A user `build()` panic is already contained per element (`behavior_commons::build_or_recover` → `ErrorView`, Flutter's `performRebuild` first catch). Every other user hook is not: `init_state` / `did_change_dependencies` run in the *child's* phase-1 drain iteration, whose `Err` arm restores the slot, re-pushes the dirty entry and `resume_unwind`s (`build_owner.rs::drain_build_scope`; pinned as propagation by `tests/inherited_dependency.rs::init_state_panic_leaves_the_slot_intact_not_a_hole`) — a deterministic `init_state` panic is a permanent retry storm. A panic inside the reconcile (`create_render_object` / `update_render_object` / `did_update_view` of a child) unwinds after `dirty_reasons.remove(&id)` and `core.clear_dirty()` ran and before `child_ids` is rewritten (`id_reconcile.rs` step 3): the parent keeps its OLD `child_ids` with freed ids while the render tree already holds the NEW render objects of elements no parent references — stranded shells paint, and the next rebuild inserts again, so ghosts accumulate. A user `dispose()` panic — the case ADR-0048 headlines — runs inline in the reconcile for unkeyed removals (`remove_finalized_inner` → `unmount` → `dispose`), after `registered_global_key.take()` and before the slot is freed. Flutter's `ComponentElement.performRebuild` has a **second** `try/catch` around `updateChild` that catches all of these; FLUI is below that floor.
2. **Retry (flui-app).** `draw_frame_for_presentation` takes the layer tree, then runs a tail (`service_child_requests`, `attach_performance_overlay`, scene). A panic in the tail drops the taken tree; `render_frame_with_sink` arms only `wake_frame()` on `Errored` (`retry_needs_repaint` deliberately false: "nothing was produced to submit" — false for this case); `run_paint` early-returns on an empty queue → the retry parks `Idle` with the screen at N-1 and the tree at N, forever. This is the root cause behind "mixed version on screen" once build is contained.
3. **Hit test (flui-app / flui-rendering).** Only the pump's ambient re-probe is gated on `any_failed` — pump-wide, so a *secondary* presentation's failure freezes the primary's hover. Pointer events between a failed frame and its retry hit-test the live tree; bounded ≤ `MAX_CONSECUTIVE_LAYOUT_FAILURES` (3) frames for retriable layout failures (structural ones poison on first occurrence), unbounded for a deterministic paint panic (paint has no poison budget — retry policy, outside this intent).
4. **Criterion 6 (flui-rendering tests).** `poison.rs` stands a poisoned node in with its last committed geometry, or `Size::ZERO`/`SliverGeometry::ZERO` if it never succeeded; ADR-0054's `GEOMETRY_DEGRADED` marks the parent. No test distinguishes the two; PR #1005's fixture passed with the poisoning step deleted (a cycle fixture poisoning the root, tight constraints, unchanged-constraints cache short-circuit).
5. **Report privacy (flui-app).** `FrameFailureKind::SegmentPanic.message` carries the panic text verbatim to the embedder's handler in every profile; `RenderError` payloads are static names/ids/counts by inspection. The on-screen `ErrorView` already withholds the message in release (`RenderErrorBox::paint` under `cfg!(debug_assertions)`) — Flutter parity; the typed report is the one channel without a profile split.

Semantics (a11y trails the screen only after a scene-submit failure, one pump) is **filed out** to the threaded-lane issue by the user's decision — see `intent.md` Corrections.

## Goals / Non-goals

**Goals**
- Every element-level mutation a user hook can abort is bounded at the child that hook belongs to; the frame never unwinds out of `build_scope` or `finalize_tree` from user code; no stranded render child survives a contained panic.
- A retry after any `Errored` outcome re-presents: the tree and the screen converge on the next pump.
- Hit test reads the committed (presented) geometry while a presentation is between a failed frame and its retry; hover-class derivations are gated per presentation.
- A test distinguishes last-good retention from the zero stand-in and goes red when the poisoning step is deleted.
- The typed report is profile-aware; contained recoveries are reported with ownership identity and causal detail, without bumping the failure streak.

**Non-goals (explicitly out of scope)**
- Semantics candidate/validate/publish (filed with the threaded raster lane; `SemanticsOwner::flush` untouched).
- A paint poison budget or any retry-policy change (`intent.md` "Not this"); the secondary-realm handler wiring (blocked).
- Routing on the `BUG:` prefix — it classifies (`internal_invariant`), it never decides containment (PANIC-POLICY owns the prefix by convention only; flui-view is `#![forbid(unsafe_code)]`, so containing cannot produce UB).
- Changing what a successful frame produces; the on-screen `ErrorView`'s release behaviour (already parity).

## Approach — A′: per-child boundaries at the three real seams, one report drain, one commit bit, a geometry shadow

### G1 — build-segment atomicity (flui-view)

**Invariant:** *a rebuild either commits a child or commits an `ErrorView` in that child's slot; a removal always frees the slot; the drain always reaches the step that rewrites `child_ids`.* Encoded as three boundaries, not a session catch:

- **Seam (a) — the phase-1 `Err(payload)` arm of `drain_build_scope`.** After `put_element`, the failing element is replaced **at its parent's slot**: `remove_subtree(id, Finalize)` then insert `ErrorView` at `(parent, slot)` through a new tree primitive `ElementTree::replace_child_with(parent, slot, view, owner)`. No re-push, no `resume_unwind`. A root element with no parent propagates (Flutter: `runApp` fatal). `StatefulBehavior::on_unmount` gates `dispose()` on `initialized`, so a `ViewState` whose `init_state` never completed is never disposed (Flutter effective parity: a throwing `initState` never reaches `dispose`). The pinned test's oracle flips from "propagates" to "substituted at the parent's slot".
- **Seam (b) — per-insert and per-update boundaries inside `reconcile_children_by_id`.** The shipped per-insert pair (`insert_reporting_child` + `InsertedChild::{Minted, Retaken}` + `discard_unannounced` | `remove_subtree(.., Finalize)`) and the per-update shape (`update_or_replace_resident`) that `sparse_children::mount_sparse_child` already uses are extracted into tree-level primitives `mount_or_substitute` / `update_or_substitute` (rule of three: sparse children, reconcile, seam (a)); `sparse_children.rs` calls them. `Minted` → `discard_unannounced` (unmount without the observation — the observer never saw a completed mount); `Retaken` (a GlobalKey retake carrying user state, announced as `Reparent`) → `remove_subtree(Finalize)`; an update panic → remove the resident + substitute. The reconcile therefore always reaches step 3, `child_ids` is always rewritten, and no stranded shell survives. Registrations a completed mount made (GlobalKey reserve, observer `element_mounted`, render adoption + parent data, interaction-lane registrations via `did_mount_render_object`) are undone by the ordinary unmount path, never by a raw slab remove.
- **Seam (c) — the user `dispose()` inside `on_unmount`.** `catch_unwind` around the hook only; `remove_finalized_inner` frees the slot and bumps the generation regardless of the outcome; both the inline reconcile removals and the finalize sweep continue. The finalize sweep reports and continues (Flutter's `finalizeTree` parity) — it never flips the frame to `Errored`.
- **One drain.** `BuildOwner::recovered_panics: Vec<RecoveredPanic { element: ElementId, parent: Option<ElementId>, view_type_id: TypeId, hook: LifecycleHook, error: FlutterError }>`, threaded through `ElementOwner` exactly like `dirty_reasons`; pushed by all three seams *and* by `build_or_recover`. Drained by the realm after the build segment into `FrameFailureReport`s with `disposition: Contained` (no streak bump). `BUG:` payloads are contained and classified (`internal_invariant`), never routed.

Failure modes A′ keeps (recorded in ADR-0048's amendment): the `ErrorView` factory itself panicking on mount is unbounded by design (it names the broken factory — `mount_sparse_child`'s stance); a `BUG:` contained at seam (b) may leave render-side residue (that boundary does not claim recovery; the report says `internal_invariant`); a deterministic `did_update_view` panic under an animated parent yields one `Contained` report per frame (the embedder throttles — the same accepted steady state as a failing submit); a `LayoutBuilder` scope root failing in seam (a) needs its scope-registry entry pruned (builder verifies).

### G2 — the retry re-presents; hit test and hover read the committed version (flui-app; PR-4 in flui-rendering)

- **Tail retry (replaces G2b).** `PresentationState` gains `segment_phase: Cell<SegmentPhase::{Build, Finalize, Pipeline, Tail, Scene}>`, written as `draw_frame_for_presentation` advances (unwind-correct: the last phase entered survives). On `Errored` with the phase past `Pipeline`, `render_frame_with_sink` arms `mark_needs_full_repaint_for(producer)` in addition to `wake_frame()`, so the retry paints and presents. The `retry_needs_repaint` comment is rewritten (its "nothing was produced to submit" justification is false for the tail case). `run_frame`'s doc is corrected: the layer tree on the semantics-`Err` arm stays in `last_layer_tree` and is submitted by the retry, not discarded.
- **The commit bit.** `PresentationState { tree_revision: Cell<TreeRevision>, presented_revision: Cell<TreeRevision> }` — a new newtype in flui-app (`epoch.rs`), distinct from the lane's per-submit `FrameEpoch` and from the failure streak (which `Idle` resets — the exact case the gate must hold). `tree_revision` bumps at segment entry when `should_run_segment()`; `presented_revision = tree_revision` on `Painted + {Presented, NoPresent}` and on `Idle`; held on `Errored` / `SurfaceStale` / `DeviceLost` / deferred-first-frame. `frame_commit_state() -> FrameCommitState::{Committed, Uncommitted { since: TreeRevision }}` replaces the pump-wide `any_failed` at the ambient re-probe (fixing the secondary-freezes-primary precision bug); hover-class derivations hold their last targets while `Uncommitted`.
- **B′ — geometry shadow (the user's choice: close the residual in this spec).** `PipelineOwner` keeps a copy-on-write shadow `FxHashMap<RenderId, ShadowGeometry { geometry: ProtocolGeometry, offset: Offset }>` populated on the FIRST geometry/offset commit of a node while the presentation is `Uncommitted` (the OLD value is inserted; later commits leave it), cleared by the realm's present acknowledgement (`PipelineOwner::acknowledge_presented()` called where `presented_revision` is set). The hit-test walk reads shadow-or-live per node; layout and paint read live. Zero cost when frames present (an empty-map clear per pump). Consistent with ADR-0062's rule that the surviving record is the cross-pass one; Flutter has no equivalent (it presents the mixed frame) — a `## Mapping decisions` entry.

### G3 — the criterion-6 fixture (flui-rendering tests, `tests/layout_poison.rs`)

Tree: root → parent (a box that lays its single child out under `BoxConstraints::loose(..)` and sizes to child + padding, whose `layout_child` error path continues on the substitute — the `DegradationProbe` route in `subtree_arena.rs`) → leaf `{ size_on_success: Cell<Size>, panic: Cell<bool>, layout_calls: Cell<u32> }` (non-root, loose constraints so S is not derivable from constraints). Pass 1: leaf returns S1 = 30×40 → assert leaf `geometry_box() == Some(S1)`, calls 1. Pass 2: `panic = true`, `size_on_success = S2 = 50×60`, `mark_needs_layout(leaf)`, run → `Poisoned`; assert leaf still S1, `layout_poison.is_poisoned(leaf)` (same-crate test or a `testing`-feature accessor — no public poison accessor is added), calls 2, parent `geometry_degraded()`. Pass 3: `panic = false` (would yield S2), `mark_needs_layout(parent)` (not the root — `layout_dirty_root` serves cached geometry on unchanged constraints; `mark_needs_layout` walks up and un-poisons only visited ancestors, so the leaf keeps its poison and the parent passes its own short-circuit), run → assert calls **still 2**, leaf **S1 not S2**, parent degraded. **Discriminator:** delete pass 2 → calls 3 and S2 → red. **Control fixture:** the leaf panics from pass 1 → `geometry_box() == None` (the ZERO stand-in), parent degraded — the same oracle with the opposite answer is what "distinguishes" means. Oracles are the render object's own call counter and `RenderNode::geometry_degraded()` (public).

### G4 — what the report carries (flui-app)

`FrameFailureKind::SegmentPanic { message: PanicText, phase: SegmentPhase, internal_invariant: bool }` with `PanicText::{Verbatim(Box<str>), Redacted}` (`Display` for `Redacted` reuses `flui_log::REDACTED_VALUE` — flui-app is a composition root that already depends on flui-log; the variant is the contract, the literal is not duplicated). Default: `Verbatim` under `cfg(debug_assertions)`, `Redacted` in release; `AppConfig::with_frame_failure_detail(FrameFailureDetail::{Redacted, Verbatim})` is the explicit release opt-in — the same profile split `RenderErrorBox` already applies on screen, so the two surfaces state one policy. `FrameFailureReport` gains `disposition: FailureDisposition::{FrameDropped, Contained}` and `FrameFailureKind::RecoveredPanic { element: ElementId, view_type_id: TypeId, hook: LifecycleHook, message: PanicText }` fed from the flui-view drain — ownership identity + causal detail for contained recoveries. `RenderError`'s five `Box<str>` variants have one production constructor (`SemanticsError` from an id-only error) — stated "by inspection" in the ADR, and `Pipeline { error }`'s `Display` goes through the same `PanicText` gate so the claim does not depend on the grep staying true. `docs/runtime-contract.toml`'s root-export manifest is updated deliberately (`FrameFailureKind` is monitored).

### PR-0 — the panic-payload classifier has one home (flui-foundation)

`flui_foundation::panic::{payload_text(&dyn Any) -> Option<&str>, is_internal_invariant(&str) -> bool}` replacing the eight production copies of the `downcast_ref::<&'static str>() / String` dance (`flui-platform` `panic_boundary.rs`, `flui-app` `ui_realm.rs` + `close_request.rs`, `flui-rendering` `entry.rs` + `cell.rs` + `subtree_arena.rs` ×2, `flui-view` `error.rs`). Returns `Option` (non-string payload → `None`, per the `close_request.rs` pin); callers keep their own fallback rendering, so no asserted message changes. A fourth copy at the new seams would be rejected; this lands first.

### Alternatives considered

- **A (session-level reconcile catch + raw child removal + `[ErrorView]` child-list replacement + `BUG:` routing + semantics stage/commit).** Rejected after adversarial review: it covered the wrong phase (`init_state` runs in the child's phase-1 iteration, not the reconcile); "raw" removal leaks four registrations the shipped `InsertedChild`/`discard_unannounced` pair already handles and ignores the `Retaken` case; a `dispose` panic mid-reconcile left the slot unfreed and the recovery re-disposed it; `resume_unwind` after the finalize sweep dropped a frame for nothing and parked the retry `Idle`; `BUG:` routing turned a string convention into control flow at one seam only; every-profile redaction flipped a ticked criterion and broke its pin; the semantics split burned two consumed side effects (`take_removed_accessibility_ids`, `focus_claimants`) and its `Idle`-commit rule recreated a11y-leads-screen after a tail failure.
- **B (versioned geometry: a second committed/presented slot per `RenderState`).** Superseded by B′ (copy-on-write shadow only while `Uncommitted`): same guarantee, zero cost on the presenting path, no per-node memory.
- **C (element-slab snapshot/undo, Compose-style — `Snapshot.takeMutableSnapshot`/`apply`/`dispose` at the state layer, verified on developer.android.com; the "throwing composition is disposed" behaviour from memory).** Rejected: the element slab, `child_ids`, the GlobalKey registry and the render tree are not MVCC; a journal needs every mutation site across two crates to cooperate — the four-write-sites failure ADR-0062 recorded. A staging `ElementTree` swap is out for the same reason (`ViewState` is not `Clone`; render adoption at mount is a side effect on the other tree).
- **D (Flutter floor verbatim: `_tryRebuild` catch-and-continue, present the frame).** Presents the torn subtree with the parent un-dirty. Masonry offers no containment (docs.rs `masonry::core::Widget`: a panic is a logic error); GPUI `Window::draw` and SwiftUI `Transaction` (animation propagation, not failure rollback) from memory, unverified — neither is a floor above Flutter's.
- **ALT-2 (presentation-fatal + clean retry: undo via the sparse report, re-push the parent, propagate).** Meets "cannot partially commit" but leaves a deterministic hook panic as a frozen window at fallback pace; the undo is the same code as A′'s, so no simplicity gain.

## Public surface & semver impact

Active-dev workspace (0.2.0), breaking allowed. **flui-foundation:** `panic::{payload_text, is_internal_invariant}` added. **flui-view:** `ElementTree::replace_child_with`, tree-level `mount_or_substitute` / `update_or_substitute` (crate-visible unless a widget-level caller needs them — api-design-lead decides), `BuildOwner::take_recovered_panics()` (+ `RecoveredPanic`, `LifecycleHook` types), `StatefulBehavior::on_unmount` gated on `initialized` (behavioural). **flui-app:** `FrameFailureKind::SegmentPanic` fields change (`message: PanicText`, `+ phase`), `FrameFailureKind::RecoveredPanic` added, `FrameFailureReport.disposition` added, `PanicText`, `SegmentPhase`, `FailureDisposition`, `FrameCommitState`, `TreeRevision`, `AppConfig::with_frame_failure_detail`; `runtime-contract.toml` manifest updated. **flui-rendering:** `PipelineOwner::acknowledge_presented()` and the shadow (PR-4); test-only accessor for `layout_poison` if the G3 fixture is not a same-crate unit test. No `flui-semantics` change.

## Pre-code maintainer verdict (recorded)

**ACCEPTABLE / RESHAPE NEEDED** (chief-architect, after the harsh-critic pass; reshapes are prerequisites, all landed in their PRs): (1) extract `sparse_children.rs`'s two boundary bodies into tree-level primitives before generalising — a third copy is rejected; (2) `RecoveredPanic` drain threaded via `ElementOwner`, no new global, no realm reach-in from flui-view; (3) `FrameFailureReport`'s new shape gated by api-design-lead; (4) `retry_needs_repaint`'s comment rewritten with the tail case, `run_frame`'s "discarded" doc corrected; (5) PR-0's helper returns `Option<&str>`. Ownership: flui-view (build seams + drain), flui-app (bit, phase, report, retry, re-probe gate), flui-rendering (G3 fixture, B′ shadow), flui-foundation (classifier). Reused: `insert_reporting_child`/`InsertedChild`/`discard_unannounced`, `remove_subtree(.., Finalize)`, `FlutterError::from_panic`, `ErrorView::build_error_view`, `RenderNode::geometry_degraded`, `GEOMETRY_DEGRADED`, `mark_needs_full_repaint_for`, `flui_log::REDACTED_VALUE`, `RenderTester`/`tests/layout_poison.rs`.

## Acceptance criteria

Each traces to intent's "What 'fixed' looks like" (as corrected): **F1** everything that reads frame state sees one committed version; **F2** a failed subtree never leaves a half-applied mutation; **F3** a test tells last-good retention from the zero stand-in and goes red without the poisoning step; **F4** a production report carries nothing sensitive by default.

- **AC1 (F2, `init_state`):** a child whose `init_state` panics is replaced by an `ErrorView` at its parent's slot in the same frame; `dispose()` is never called on that state; the frame is `Painted`; the parent's render-child count is unchanged; red before PR-2b (the panic propagates — the flipped pin).
- **AC2 (F2, reconcile insert):** a `Row` of 10 children where child 7's `create_render_object` panics → one `ErrorElement` at slot 7, nine siblings intact and laid out, `child_ids` rewritten (no freed ids), the `TreeObserver`'s mount/unmount events pair, the parent's render-child count is 10; red before: the panic escapes.
- **AC3 (F2, ghost regression):** after AC2's frame, two further parent rebuilds leave the parent's render-child count at 10 and the slab count constant (no stranded shells); red before: the count grows per rebuild.
- **AC4 (F2, `Retaken`):** a GlobalKey-retaken child whose mount panics is removed with `Finalize` (key unregistered, observer balanced); red before.
- **AC5 (F2, `did_update_view` / `update_render_object`):** an update panic removes the resident and substitutes; siblings intact; red before.
- **AC6 (F2, `dispose`):** a `dispose()` panic (a) inline during a reconcile removal and (b) in the finalize sweep frees the slot, bumps the generation, the sweep/reconcile continues, the frame is `Painted`, and one `Contained` report with `hook: Dispose` is delivered; red before: (a) the slot survives and a second `dispose` fires, (b) the panic escapes.
- **AC7 (F2, `BUG:` classified not routed):** a `BUG:`-prefixed panic in a lifecycle hook is contained exactly like any other and reported with `internal_invariant: true`.
- **AC8 (F1, tail retry):** a panic injected after `run_frame` returned `Ok` (tail probe) yields `Errored`; the very next pump paints and presents (`Painted { Presented }`), and the screen's scene equals a fresh owner's; red before: the retry parks `Idle`.
- **AC9 (F1, commit bit):** after `Errored → retry parks Idle` (no repaint armed by construction in the test), `frame_commit_state()` is `Uncommitted`; after a `Painted { Presented }` it is `Committed`; a secondary presentation's failure leaves the primary `Committed` and its ambient re-probe runs; red before: the streak resets on `Idle` / `any_failed` is pump-wide.
- **AC10 (F1, B′):** a boundary whose child moved (layout committed a new offset) in a frame that then failed in paint: a pointer event dispatched before the retry hits the target at its PRESENTED position (the shadow), a layout query returns the live one; after the retry presents, the shadow is empty and hit test reads live; red before: the event hits the live position. Plus: the shadow holds the OLD value across several unpresented commits (the first write wins), and a presenting pump costs one empty-map clear (assert the map is empty after a clean frame).
- **AC11 (F3):** the G3 fixture as specified: pass 3 asserts `layout_calls == 2` and leaf `== S1`, parent `geometry_degraded()`; the control fixture asserts `geometry_box() == None`; the test file's doc states the discriminator (delete pass 2 → red), and the PR shows the red output.
- **AC12 (F4):** in a release build (`cargo test --release` on the relevant test, or a `cfg`-toggled unit) `SegmentPanic.message` is `PanicText::Redacted` by default and `Verbatim` with `FrameFailureDetail::Verbatim`; in a debug build it is `Verbatim` by default (`frame_failure_containment` keeps asserting the message reaches the report); `RecoveredPanic` follows the same rule; `Pipeline { error }`'s `Display` never emits a `Box<str>` payload verbatim in release.
- **AC13 (dev diagnostics, already ✅ — kept):** `RecoveredPanic` carries `element`, `view_type_id`, `hook`; `SegmentPanic` carries `phase`; both carry `address`.
- **AC14 (accounting):** ADR-0048 amended — "Build-side containment" (parity with `performRebuild`'s second catch and `finalizeTree`'s continue; per-child substitution vs Flutter's whole-element `_tryRebuild` drop = improvement; `init_state` substitution at the failing element's own slot vs Flutter's nearest-`ComponentElement` replacement = narrower improvement; `BUG:` contain-and-classify), "Retry and last-good retention" (tail case; the "quiescent ending" paragraph corrected), "Privacy" (profile-aware, both surfaces), "Consistency audit" rewritten (re-established: build seams, finalize, tail retry, hit test via the shadow; residual: the realm pre-phase, secondary-realm wiring, semantics candidate filed to the lane issue); `crates/flui-rendering/ARCHITECTURE.md` `## Mapping decisions` entry for the shadow; PANIC-POLICY pointer to the classifier; `docs/runtime-contract.toml`.

## Risks & open questions

- Seam (b)'s extraction touches the reconcile hot path: measure `bench`es that cover reconcile (`rg -n "reconcile" crates/flui-view/benches`) before/after; a per-insert `catch_unwind` is a landing-pad, not a runtime cost, but the `InsertedChild` report adds a move per insert.
- The shadow's "first write wins" must be keyed on the presentation's `Uncommitted` state at commit time; a realm with two presentations sharing one `PipelineOwner` does not exist (ADR-0027), but the PR states the assumption.
- `LayoutBuilder` scope roots at seam (a): the scope registry must not keep a pruned root — builder verifies with a test.
- A `Contained` report per frame under an animated deterministic failure is a report storm by design; the embedder's handler throttles (documented), no streak bump.

## Ordered tasks (each PR independently green and one kind of change)

1. **PR-0** — flui-foundation classifier + mechanical migration of the eight copies (no message changes; the `close_request.rs` non-string pin stays).
2. **PR-1** — G3 fixture pair in `tests/layout_poison.rs` (AC11) + one line in ARCHITECTURE.md's `catch_unwind` entry naming the oracle.
3. **PR-2a** — flui-view: extract `mount_or_substitute` / `update_or_substitute`; seam (c) inside `on_unmount` with the slot freed regardless; `dispose` gated on `initialized`; `RecoveredPanic` drain through `ElementOwner` (AC6, AC7 partly).
4. **PR-2b** — flui-view: seams (a) + (b) on the primitives; the flipped pin; AC1–AC5, AC7; ADR-0048 "Build-side containment" section.
5. **PR-3** — flui-app: `SegmentPhase` + tail repaint arm (AC8); `TreeRevision` + `FrameCommitState` replacing `any_failed` at the re-probe (AC9); `PanicText` / `FrameFailureDetail` / `disposition` / `RecoveredPanic` + drain forwarding (AC12, AC13); ADR-0048 sections; `runtime-contract.toml`.
6. **PR-4** — flui-rendering: geometry shadow + `acknowledge_presented()` + the hit-test read (AC10); ARCHITECTURE.md mapping entry; ADR-0048 "Consistency audit" hit-test line closed.
7. **PR-5** — close: #561 comment with the six criteria mapped; issue filed for semantics candidate/commit at the lane; issue filed for the paint poison budget.

## Links

- `intent.md` (same slug); ADR-0048, ADR-0027, ADR-0054, ADR-0062; `docs/PANIC-POLICY.md`.
- Memory: `a-poisoned-frame-must-not-clear-what-it-cannot-commit`, `degraded-geometry-must-be-sticky`, `viewport-atomicity-and-center-model`, `flutter-isolates-a-throwing-paint-flui-poisons-the-frame`, `paint-queue-is-the-cross-pass-record`.
- Chief-architect rulings: `.claude/agent-memory/rust-studio-chief-architect/project_561_frame_failure_rulings.md`.
