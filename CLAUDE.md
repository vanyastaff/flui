# CLAUDE.md

The agent guide for this repository lives in [`AGENTS.md`](AGENTS.md) (the cross-tool
standard, shared with other agent runtimes). It is imported below so Claude Code
auto-loads it — keep the substance in `AGENTS.md`, not here, to avoid drift.

@AGENTS.md
