//! Headless GPU adapter support for testing/CI.
//!
//! Headless device creation is provided by [`GpuDevice::new_headless()`](super::gpu_device::GpuDevice::new_headless).
//! This module exists for future headless-specific utilities (e.g., read-back,
//! offscreen render targets).
