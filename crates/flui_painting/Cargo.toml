[package]
name = "flui_painting"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Canvas-based painting abstraction for FLUI - records drawing commands into DisplayList"
repository.workspace = true

[dependencies]
flui_types = { path = "../flui_types" }

# Note: Math operations use Matrix4 from flui_types, which internally uses glam
# No direct glam dependency needed here
thiserror = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
