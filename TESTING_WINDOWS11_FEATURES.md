# Как проверить Windows 11 функции

## Запуск демо

```bash
cargo run --example windows11_demo
```

## Что проверять визуально

### 1. Mica Backdrop (Полупрозрачный фон) ✨

**Как проверить:**
- Посмотрите на фон окна (область под заголовком)
- Фон должен быть **полупрозрачным**
- Через окно должны **просвечивать обои рабочего стола** с эффектом размытия
- Это выглядит как "матовое стекло" (frosted glass)

**Если НЕ работает:**
- Фон будет просто белым/серым
- Никакой прозрачности не будет видно

**Проверка:** Переместите окно на разные части экрана - фон должен меняться в зависимости от обоев под окном.

---

### 2. Dark Mode Title Bar (Тёмный заголовок) 🌙

**Как проверить:**
- Посмотрите на заголовок окна (верхняя полоска с названием)
- Заголовок должен быть **тёмным** (почти чёрным)
- Текст заголовка "Windows 11 Features Demo" должен быть **белым**
- Кнопки (свернуть, развернуть, закрыть) должны быть светлыми на тёмном фоне

**Если НЕ работает:**
- Заголовок будет светлым (белым)
- Текст будет чёрным

---

### 3. Custom Title Bar Color (Синий оттенок заголовка) 🔵

**Как проверить:**
- Посмотрите внимательно на заголовок
- Он должен иметь **тёмно-синий оттенок** (не чисто чёрный, а с синевой)
- Цвет: RGB(20, 30, 50) - очень тёмный синий

**Если НЕ работает:**
- Заголовок будет просто чёрным без синего оттенка

**Примечание:** Оттенок очень тонкий, лучше видно на ярком экране.

---

### 4. Rounded Corners (Скруглённые углы) ⚪

**Как проверить:**
- Посмотрите на **углы окна** (все 4 угла)
- Углы должны быть **плавно скруглёнными** (не острыми)
- Скругление небольшое, но заметное

**Если НЕ работает:**
- Углы будут острыми (90 градусов)

**Проверка:** Сравните с обычным окном Проводника - у всех окон Windows 11 скруглённые углы.

---

### 5. Snap Layouts (Сетка привязки окна) 📐

**Как проверить:**
1. Наведите курсор мыши на кнопку **"Развернуть"** (средняя кнопка в правом верхнем углу)
2. **НЕ нажимайте**, просто наведите и подождите 0.5 секунды
3. Должна появиться **всплывающая панель** с вариантами расположения окна

**Что вы должны увидеть:**
- Панель с 6 вариантами раскладки окна
- Можно выбрать: полный экран, половина экрана, треть экрана, и т.д.
- Это новая функция Windows 11

**Если НЕ работает:**
- Панель не появится
- Будет обычное поведение кнопки

**Примечание:** Snap Layouts включён по умолчанию в Windows 11, мы просто не блокируем его.

---

## Проверка в консоли

При запуске в консоли должен быть такой вывод:

```
🪟 Windows 11 Features Demo
━━━━━━━━━━━━━━━━━━━━━━━━━━━

    Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.50s
     Running `target\debug\examples\windows11_demo.exe`
✅ Window created successfully!
📋 Window HWND: HWND(0x...)
🎨 Applying Windows 11 features...
  ✓ Setting Mica backdrop
  ✓ Enabling dark mode
  ✓ Setting rounded corners
  ✓ Setting custom title bar color (dark blue)
✨ All features applied!

What you should see:
  • Translucent Mica backdrop showing desktop wallpaper
  • Dark title bar with dark blue color
  • Rounded window corners
  • Hover over maximize button to see Snap Layouts

Close the window to exit
Platform ready!
Starting Windows message loop
Created window HWND ... - 1000x700 at (32767, 32767) - scale: 1
```

Если все эти сообщения есть - функции были применены.

---

## Системные требования

### Для ВСЕХ функций (Mica + всё остальное):
- **Windows 11 Build 22000+** (выпущена в октябре 2021)

### Для Dark Mode (без Mica):
- **Windows 10 Build 17763+** (October 2018 Update)

### Проверка версии Windows:
```bash
winver
```
Должно быть: "Version 22H2 (OS Build 22621...)" или новее

---

## Если функции НЕ работают

### Проблема: Mica backdrop не показывается

**Причины:**
1. Windows 10 вместо Windows 11
2. Старая сборка Windows 11 (до Build 22000)
3. Прозрачность отключена в настройках

**Решение:**
- Обновите Windows 11
- Проверьте: Settings → Personalization → Colors → Transparency effects (должно быть ON)

### Проблема: Вообще ничего не работает

**Решение:**
```bash
# Проверьте что процесс запущен
tasklist | findstr windows11_demo

# Если не запущен, попробуйте снова
cargo run --example windows11_demo

# Проверьте логи
RUST_LOG=debug cargo run --example windows11_demo
```

---

## Сравнение: До и После

### Обычное окно (без наших функций):
- ❌ Непрозрачный белый фон
- ❌ Светлый заголовок
- ❌ Острые углы (на Windows 10)

### Наше окно (с Windows 11 функциями):
- ✅ Полупрозрачный Mica backdrop
- ✅ Тёмный заголовок с синим оттенком
- ✅ Скруглённые углы
- ✅ Snap Layouts работают

---

## Дополнительно: Проверка через PowerShell

Можно проверить что DWM атрибуты были установлены:

```powershell
# Получить HWND окна (выведен в консоли при запуске)
# Например: HWND(0x12AB34)

# Проверить что окно существует
Get-Process windows11_demo | Select-Object MainWindowHandle
```

---

## Итог

Если вы видите хотя бы **3 из 5 функций** - всё работает отлично! 🎉

Самое заметное - это **Mica backdrop** (полупрозрачность) и **Dark Mode** (тёмный заголовок).
