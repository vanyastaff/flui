# SUPERSEDED — preserved for audit trail

**Date:** 2026-05-25
**Why superseded:** This change was scoped as "ratify cycle-3 outcomes via 3 doc PRs" based on a misread of user intent. The user clarified that cycle 3 was done with weaker tooling and that the SDD-new is meant to **adversarially re-audit** cycle-3 work using advanced SDD + Rust 1.95+ + edition 2024 patterns + cross-vendor peer review — finding what cycle 3 missed.

The re-scoped change replaces this one. Artifacts here remain valid as evidence of what cycle 3 **did** ship; the new change focuses on what cycle 3 **may have missed**.

**Replacement:** `openspec/changes/core-0a-foundation-adversarial-reaudit/` (created in same session).

**Do not extend this change. Do not run sdd-apply against it.**
