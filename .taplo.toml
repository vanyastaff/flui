# TOML formatting (`taplo fmt`); only the settings that differ from taplo's
# defaults.
include = ["**/*.toml"]
exclude = ["**/target/**", ".flutter/**", ".gpui/**", ".claude/**"]

[formatting]
column_width = 100
indent_string = "    "
