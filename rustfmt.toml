# Only the settings that differ from rustfmt's defaults. `edition` and
# `style_edition` are spelled out so rustfmt run outside cargo (an editor on
# save) formats the same way `cargo fmt` does.
edition = "2024"
style_edition = "2024"
use_try_shorthand = true
use_field_init_shorthand = true
