[package]
name = "flui-hot-reload-lifecycle-fixture"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false
description = "Dev-only app_plugin! fixture for flui-hot-reload's reload-lifecycle integration test; never shipped"

[lib]
crate-type = ["rlib", "cdylib"]
name = "flui_hot_reload_lifecycle_fixture"

[dependencies]
flui-hot-reload = { path = "../../crates/flui-hot-reload", features = ["app-plugin"] }
flui-layer = { path = "../../crates/flui-layer" }
flui-view = { path = "../../crates/flui-view" }

[lints]
workspace = true
