# Итог Исследования GPUI

**Дата:** 2026-01-24  
**Вопрос:** Как GPUI связывает platform и interaction? Можно ли использовать f32 для дельт и скоростей?

---

## Главные Находки

### 1. GPUI НЕ использует дженерики в событиях! 🎯

```rust
// GPUI определяет Point<T> как дженерик, НО:
pub struct MouseDownEvent {
    pub position: Point<Pixels>,  // ВСЕГДА Point<Pixels>, не Point<U>!
}

// Это решает проблему 592 ошибок!
```

### 2. GPUI имеет более простую архитектуру

```
GPUI:  OS → Platform → Window (2 слоя)
FLUI:  OS → Platform → App → Interaction (3 слоя)
```

Window в GPUI сам обрабатывает события напрямую, без отдельного interaction слоя.

### 3. Конвертация на границе платформы

GPUI конвертирует координаты в `Point<Pixels>` **сразу** при получении от OS:

```rust
// platform/windows/events.rs
fn handle_mouse_msg(device_x: i32, device_y: i32) {
    // Сразу конвертируем в логические пиксели
    let logical = Point {
        x: Pixels(device_x as f32 / scale_factor),
        y: Pixels(device_y as f32 / scale_factor),
    };
    
    // Событие с конкретным типом
    window.dispatch_event(PlatformInput::MouseDown(MouseDownEvent {
        position: logical,  // Point<Pixels>
    }));
}
```

---

## Ответ на Твой Вопрос

### Использовать f32 для дельт и скоростей?

**НЕТ! Нельзя использовать f32!** ❌

У вас уже есть правильные типы в `flui_types`:

#### Для дельт движения:

```rust
// ✅ ПРАВИЛЬНО: PixelDelta сохраняет семантику
pub struct PixelDelta(pub f32);  // Из flui_types/src/geometry/units.rs

let movement = Offset::<PixelDelta>::new(
    delta_px(5.0),
    delta_px(-3.0)
);

// ❌ НЕПРАВИЛЬНО: f32 теряет информацию
let bad_movement = Offset::<f32>::new(5.0, -3.0);  // 5 чего?
```

#### Для скоростей:

```rust
// ✅ ПРАВИЛЬНО: Velocity уже определена правильно!
pub struct Velocity {
    pub pixels_per_second: Offset<Pixels>,  // Из flui_types/src/gestures/velocity.rs
}

let velocity = Velocity::new(Offset::new(px(100.0), px(50.0)));
// Значение: 100 пикселей/секунду вправо, 50 пикселей/секунду вниз

// ❌ НЕПРАВИЛЬНО: f32 не показывает единицы
let bad_velocity = Offset::<f32>::new(100.0, 50.0);  // Потеряна размерность!
```

---

## Почему НЕ f32?

### 1. Потеря Типобезопасности

```rust
// С f32 можно случайно смешать разные величины:
let position = Offset::<f32>::new(100.0, 200.0);
let velocity = Offset::<f32>::new(50.0, 25.0);
let bad_result = position + velocity;  // ❌ Компилируется, но семантически неверно!

// С правильными типами:
let position = Offset::<Pixels>::new(px(100.0), px(200.0));
let velocity = Velocity::new(Offset::new(px(50.0), px(25.0)));
let bad = position + velocity.pixels_per_second;  // ❌ Ошибка компиляции! Нельзя складывать позицию и скорость
```

### 2. Потеря Документации в Коде

```rust
// f32 - что это?
fn calculate_delta(a: Offset<f32>, b: Offset<f32>) -> Offset<f32> { ... }
// Это дельта? Скорость? Позиция? Непонятно!

// Типы говорят сами за себя:
fn calculate_delta(
    from: Offset<Pixels>, 
    to: Offset<Pixels>
) -> Offset<PixelDelta> { ... }
// Понятно: принимает две позиции, возвращает дельту
```

### 3. Потеря Размерности

```rust
// Velocity - это НЕ безразмерная величина!
// Это пиксели ПЕР СЕКУНДУ (pixels/second)

// ❌ f32 скрывает размерность
let velocity: Offset<f32> = Offset::new(100.0, 50.0);
// 100.0 чего? На что? Неясно!

// ✅ Velocity явно показывает единицы
pub struct Velocity {
    pub pixels_per_second: Offset<Pixels>,  // px/s !
}
```

---

## Семантика Типов

| Тип | Физический Смысл | Пример |
|-----|------------------|--------|
| `Pixels` | Абсолютная координата | Позиция курсора: (100px, 200px) |
| `PixelDelta` | Относительное смещение | Движение: (+5px, -3px) |
| `Velocity` | Скорость изменения | Velocity: (100px/s, 50px/s) |
| `f32` | ❌ Нет физического смысла | ❌ Не использовать для единиц! |

---

## Правильная Структура Событий для FLUI

Основываясь на анализе GPUI:

```rust
// ✅ ПРАВИЛЬНО: События без дженериков, с конкретными типами

pub struct PointerEventData {  // БЕЗ <U>!
    pub pointer_id: PointerId,
    pub pointer_type: PointerType,
    
    // Где находится курсор (абсолютная позиция)
    pub position: Offset<Pixels>,
    
    // На сколько сдвинулся (относительная дельта)
    pub movement: Offset<PixelDelta>,
    
    // С какой скоростью движется (пиксели/секунду)
    pub velocity: Velocity,
    
    pub buttons: PointerButtons,
    pub pressure: f32,  // f32 OK здесь - это просто число от 0.0 до 1.0
}

pub struct ScrollEventData {  // БЕЗ <U>!
    // Величина прокрутки (относительная дельта)
    pub delta: Offset<PixelDelta>,
    pub phase: ScrollPhase,
}

pub struct GestureBinding {  // БЕЗ <U>!
    hit_test_cache: HashMap<PointerId, HitTestResult>,
    velocity_tracker: VelocityTracker,  // Использует Offset<Pixels> внутри
}
```

---

## План Исправления 592 Ошибок

### Шаг 1: Убрать дженерики из событий

```diff
- pub struct PointerEventData<U: Unit = Pixels> {
-     pub position: Offset<U>,
-     pub movement: Offset<U>,
- }

+ pub struct PointerEventData {
+     pub position: Offset<Pixels>,
+     pub movement: Offset<PixelDelta>,
+     pub velocity: Velocity,
+ }
```

### Шаг 2: Убрать дженерики из GestureBinding

```diff
- pub struct GestureBinding<U: Unit = Pixels> {
-     cache: HashMap<PointerId, HitTestResult<U>>,
-     tracker: VelocityTracker<U>,
- }

+ pub struct GestureBinding {
+     cache: HashMap<PointerId, HitTestResult>,
+     tracker: VelocityTracker,
+ }
```

### Шаг 3: Обновить VelocityTracker

```diff
- pub struct VelocityTracker<U: Unit> {
-     samples: Vec<(Duration, Offset<U>)>,
- }

+ pub struct VelocityTracker {
+     samples: Vec<(Duration, Offset<Pixels>)>,
+ }

- impl<U: Unit> VelocityTracker<U> {
-     pub fn estimate(&self) -> Velocity<U> { ... }  // ERROR!
- }

+ impl VelocityTracker {
+     pub fn estimate(&self) -> Velocity { ... }  // OK!
+ }
```

### Шаг 4: Обновить обработчики событий

```diff
- impl<U: Unit> GestureBinding<U> {
-     pub fn handle_pointer_event<U: Unit>(&mut self, event: PointerEventData<U>) {
-         self.cache.insert(...);  // ERROR: не может вывести U
-     }
- }

+ impl GestureBinding {
+     pub fn handle_pointer_event(&mut self, event: PointerEventData) {
+         self.cache.insert(...);  // OK: event.position это Offset<Pixels>
+     }
+ }
```

---

## Что Взять от GPUI

### ✅ Взять:

1. **Монomorphic события** - `MouseDownEvent` без дженериков
2. **Конвертация на границе** - Platform сразу даёт `Point<Pixels>`
3. **Простота** - Меньше слоёв = меньше сложности

### ❌ НЕ Брать:

1. Отсутствие модульности (у GPUI всё в Window)
2. Отсутствие W3C совместимости
3. Нет разделения между Platform и Interaction

---

## Итоговая Рекомендация

### Для flui_interaction:

```rust
// 1. Убрать все дженерики <U: Unit> из публичных API

// 2. Использовать ТОЛЬКО эти типы:
- Позиция:  Offset<Pixels>
- Движение: Offset<PixelDelta>
- Скорость: Velocity (уже содержит Offset<Pixels>)

// 3. НИКОГДА не использовать:
- Offset<f32>  ❌
- Point<f32>   ❌
- Size<f32>    ❌
```

### Преимущества:

✅ 0 ошибок компиляции вместо 592  
✅ Типобезопасность (нельзя смешать позицию и скорость)  
✅ Самодокументирующийся код  
✅ Сохранена размерность (px, px/s)  
✅ Совместимость с GPUI подходом  

---

## Созданные Документы

1. **`GPUI_PLATFORM_INTERACTION_ANALYSIS.md`** - Полный анализ GPUI (диаграммы, примеры кода)
2. **`FLUI_TYPE_SYSTEM_GUIDE.md`** - Руководство по типам FLUI (таблицы, правила)
3. **`ТИПЫ_FLUI_КРАТКО.md`** - Краткая памятка на русском
4. **`ИТОГ_ИССЛЕДОВАНИЯ_GPUI.md`** - Этот документ

---

## Заключение

**Вопрос:** Можно ли использовать `Offset<f32>` для дельт и скоростей?  
**Ответ:** **НЕТ!** ❌

У вас уже есть правильные типы:
- `PixelDelta` для дельт
- `Velocity` для скоростей

Используйте их! Это даст:
- Типобезопасность
- Самодокументирующийся код
- Сохранение размерности
- 0 ошибок компиляции

**GPUI показывает:** монomorphic типы в событиях решают проблему генериков.

**FLUI должен применить:** убрать `<U: Unit>` из `flui_interaction`, использовать конкретные типы!

---

**Готово! Можно исправлять 592 ошибки! 🚀**
