# FLUI Style System

## Обзор

FLUI поддерживает три разных стиля написания UI кода. Каждый стиль оптимизирован для разных предпочтений и сценариев использования.

## Три Стиля

### 1. **Macro Style** - Максимально Декларативный

**Философия:** Минимум шаблонного кода, максимум декларативности, Flutter/SwiftUI-подобный синтаксис.

**Импорт:**
```rust
use flui_widgets::style::macros::prelude::*;
```

**Пример:**
```rust
scaffold! {
    background_color: Color::WHITE,
    body: column![
        text! { data: "Hello", size: 24.0 },
        sized_box! { height: 16.0 },
        row![
            text! { data: "A" },
            text! { data: "B" }
        ]
    ]
}
```

**Когда использовать:**
- ✅ Быстрое прототипирование
- ✅ Простые UI с большим количеством текста и разметки
- ✅ Когда нужен Flutter-like синтаксис
- ✅ Демо и примеры

**Плюсы:**
- Минимальный шаблонный код
- Очень компактно
- Легко читать структуру

**Минусы:**
- Меньше подсказок IDE
- Требует знания макросов
- Может быть менее знакомо для Rust-разработчиков

### 2. **Builder Style** - Традиционный Rust

**Философия:** Явное лучше неявного, полная поддержка IDE, традиционные Rust паттерны.

**Импорт:**
```rust
use flui_widgets::style::builder::prelude::*;
```

**Пример:**
```rust
Scaffold::builder()
    .background_color(Color::WHITE)
    .body(
        Column::builder()
            .child(
                Text::builder()
                    .data("Hello")
                    .size(24.0)
                    .build()
            )
            .child(SizedBox::builder().height(16.0).build())
            .build()
    )
    .build()
```

**Когда использовать:**
- ✅ Production код
- ✅ Команды, предпочитающие традиционный Rust
- ✅ Когда нужна максимальная поддержка IDE
- ✅ Сложные конфигурации с множеством параметров

**Плюсы:**
- Отличный autocomplete в IDE
- Традиционный Rust код
- Явные вызовы методов
- Нет "магии"

**Минусы:**
- Более многословно
- Больше `.build()` вызовов
- Может быть избыточным для простых случаев

### 3. **Hybrid Style** - Лучшее из Обоих (Рекомендуется)

**Философия:** Макросы для простых повторяющихся паттернов, билдеры для сложной конфигурации.

**Импорт:**
```rust
use flui_widgets::style::hybrid::prelude::*;
```

**Пример:**
```rust
Scaffold::builder()
    .background_color(Color::WHITE)
    .body(
        column![
            text! { data: "Hello", size: 24.0 },
            sized_box! { height: 16.0 },
            Button::builder("Click me")
                .color(Color::BLUE)
                .on_pressed(|| println!("Clicked!"))
                .build()
        ]
    )
    .build()
```

**Когда использовать:**
- ✅ **Рекомендуется по умолчанию**
- ✅ Сбалансированные проекты
- ✅ Когда нужна гибкость
- ✅ Реальные приложения

**Плюсы:**
- Баланс компактности и явности
- Используй что подходит для задачи
- Прагматичный подход
- Хорошая поддержка IDE для сложных частей

**Минусы:**
- Нужно знать оба подхода
- Может быть непоследовательным, если не следовать соглашениям

## Сравнение Стилей

| Аспект | Macro | Builder | Hybrid |
|--------|-------|---------|--------|
| **Компактность** | ⭐⭐⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐⭐ |
| **IDE Support** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **Явность** | ⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **Легкость обучения** | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **Гибкость** | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |

## Рекомендации

### Для Разных Типов Кода

**Простой UI (списки, формы, текст):**
```rust
use flui_widgets::style::macros::prelude::*;

column![
    text! { data: "Title", size: 24.0 },
    text! { data: "Subtitle", size: 14.0 },
    sized_box! { height: 16.0 },
    // ...
]
```

**Сложные виджеты с логикой:**
```rust
use flui_widgets::style::builder::prelude::*;

Button::builder("Submit")
    .color(Color::BLUE)
    .width(200.0)
    .on_pressed(move || {
        // Complex logic here
        validate_form();
        submit_data();
    })
    .disabled(form_has_errors)
    .build()
```

**Реальное приложение (hybrid):**
```rust
use flui_widgets::style::hybrid::prelude::*;

scaffold! {
    background_color: Color::WHITE,
    body: column![
        // Simple markup with macros
        text! { data: "Login", size: 32.0 },
        sized_box! { height: 24.0 },

        // Complex widgets with builders
        TextField::builder()
            .placeholder("Email")
            .validator(email_validator)
            .on_changed(|text| email.set(text))
            .build(),

        sized_box! { height: 16.0 },

        Button::builder("Submit")
            .color(Color::BLUE)
            .on_pressed(submit_handler)
            .build()
    ]
}
```

## Соглашения

### В Hybrid Style

Используйте макросы для:
- ✅ `text!` - текстовые виджеты
- ✅ `sized_box!` - отступы и пробелы
- ✅ `column!`/`row!` - простые макеты
- ✅ `scaffold!` - базовая структура экрана

Используйте билдеры для:
- ✅ Виджеты с callback'ами (кнопки, поля ввода)
- ✅ Виджеты с комплексной конфигурацией
- ✅ Кастомные виджеты с бизнес-логикой
- ✅ Условная логика

## Примеры

### Полное Приложение

См. [examples/style_comparison.rs](examples/style_comparison.rs) для полного сравнения трех стилей side-by-side.

```bash
cargo run --example style_comparison
```

**Результат:** Три карточки, показывающие идентичный UI, написанный в трех разных стилях.

### Другие Примеры

- `examples/macro_showcase.rs` - Демонстрация всех макросов
- `examples/profile_card_macros.rs` - Реальный пример с макросами
- См. [MACRO_IMPROVEMENTS.md](MACRO_IMPROVEMENTS.md) для деталей

### Миграция Между Стилями

Все три стиля совместимы - можно смешивать импорты:

```rust
// Начали с macro style
use flui_widgets::style::macros::prelude::*;

// Добавили builder для сложного виджета
use flui_widgets::Button;

// Работает отлично!
column![
    text! { data: "Hello" },
    Button::builder("Click")
        .color(Color::BLUE)
        .build()
]
```

## FAQ

### Можно ли смешивать стили?

Да! Все три стиля совместимы. Hybrid style именно для этого.

### Какой стиль быстрее?

Все три стиля генерируют одинаковый код после компиляции. Производительность идентична.

### Какой стиль рекомендуется?

**Hybrid style** рекомендуется для большинства проектов. Он дает гибкость использовать нужный подход для каждой ситуации.

### Нужно ли выбирать один стиль?

Нет, но последовательность помогает читаемости:
- Для прототипов: **Macro**
- Для библиотек: **Builder**
- Для приложений: **Hybrid**

### Как работает style prelude?

Каждый стиль - это просто модуль с re-export'ами:
- **macros**: экспортирует макросы
- **builder**: экспортирует только типы (без макросов)
- **hybrid**: экспортирует и то, и другое

## Будущие Улучшения

### Планируется

- [ ] Больше макросов (container!, padding!, center!)
- [ ] Feature flags для выбора стиля по умолчанию
- [ ] Макрос для условного рендеринга
- [ ] Spread operator для списков виджетов

### Feature Flags (будущее)

```toml
[dependencies]
flui_widgets = { version = "0.1", features = ["style-macros"] }  # Default to macro style
flui_widgets = { version = "0.1", features = ["style-builder"] } # Default to builder style
flui_widgets = { version = "0.1" }  # Default to hybrid style
```

## Заключение

Система стилей FLUI дает гибкость выбора подхода, который лучше всего подходит для вашего проекта и команды. Используйте:

- **Macro** для быстрого прототипирования и простого UI
- **Builder** для традиционного Rust кода с максимальной поддержкой IDE
- **Hybrid** (рекомендуется) для реальных приложений - лучшее из обоих миров

Все три стиля генерируют одинаковый эффективный код. Выбирайте то, что делает ваш код наиболее читаемым и поддерживаемым! 🚀
